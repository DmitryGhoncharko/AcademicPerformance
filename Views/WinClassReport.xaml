<Window x:Class="AcademicPerformance.Views.WinClassReport"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Отчет по классу" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Отчет по классу" FontSize="20" FontWeight="Bold" Margin="10" />

        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="1" Margin="10">
            <StackPanel x:Name="ReportContentPanel" />
        </ScrollViewer>

        <Button Grid.Row="2" Content="Закрыть" HorizontalAlignment="Right" Width="100" Margin="10" Click="Close_Click" />
    </Grid>
</Window>